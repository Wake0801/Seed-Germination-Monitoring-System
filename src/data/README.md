# Data Pipeline

This folder will contain data preparation scripts.

Core baseline files:
- `build_crops.py`: parse XML, crop seed patches, and write metadata.
- `split_by_sequence.py`: create train/val/test splits by `sequence_id`.

